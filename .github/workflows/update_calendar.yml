name: Teste calendário esportes

on:
  schedule:
    - cron: '0 0,12 * * *'  # Todos os dias às 00h00 e 12h00 UTC
  workflow_dispatch:  # Permite execução manual

permissions:
  contents: write  # Para poder commitar o calendar.ics
  id-token: write

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      # 🔹 Checkout do repositório
      - name: Checkout código
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # 🔹 Configurar Python 3.11
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: 3.11

      # 🔹 Instalar dependências Python
      - name: Instalar dependências
        run: |
          python -m pip install --upgrade pip
          pip install requests ics pytz pillow pytesseract

      # 🔹 Instalar Tesseract OCR no Ubuntu
      - name: Instalar Tesseract
        run: |
          sudo apt-get update
          sudo apt-get install -y tesseract-ocr tesseract-ocr-por

      # 🔹 Executar script de teste
      - name: Executar script generate_ics_teste.py
        run: python generate_ics_teste.py

      # 🔹 Commit e push do calendar.ics
      - name: Commit e push calendar.ics
        run: |
          git config --global user.name "github-actions"
          git config --global user.email "actions@github.com"
          git add calendar.ics
          git commit -m "Teste ICS - $(date -u +"%Y-%m-%dT%H:%M:%SZ")" || echo "Sem mudanças para commitar"
          git push
